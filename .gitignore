.DS_Store
node_modules/
*.log

# Учётные данные администратора — только на сервере, никогда в репозитории
data/admin.php
data/.login_attempts

# Резервные копии контента делает сам сайт
data/backups/

# Картинки, загруженные через админку, живут на боевом сервере
assets/img/uploads/*
!assets/img/uploads/.htaccess

# Сборка под свой домен — дубль docs/, в репозитории не нужна
build-*/

# Адрес FTP и корень сайта — реквизиты хостинга, репозиторий публичный
deploy/ftp.env

# Отметка о последней отправке IndexNow — только на сервере
data/.indexnow

# Исходный фотоархив (988 МБ) — рабочий материал, не часть сайта
archive/

# Личные заметки по сессиям Claude Code
ЧАТЫ.md
